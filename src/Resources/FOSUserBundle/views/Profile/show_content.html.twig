{% trans_default_domain 'FOSUserBundle' %}


<div class="container">    
   
        <div id="loginbox" style="margin-top:100px;" class="mainbox col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2">                    
            <div class="panel panel-info" >   
                <div class="alert alert-primary" role="alert">
  <strong>Hello</strong> Votre mot de Passe a bien été réinitialisé, veuillez-vous connecter
  <button type="button" class="close" data-dismiss="alert" aria-label="Close">
    <span aria-hidden="true">&times;</span>
  </button>
</div>

<div class="fos_user_user_show">
    <p>{{ 'profile.show.username'|trans }}: {{ user.username }}</p>
    <p>{{ 'profile.show.email'|trans }}: {{ user.email }}</p>
</div>
<div style="margin-left:80px">
 <a href="{{ path('index') }}"<button type="submit" id="_submit" name="_submit" class="btn btn-primary">{{ button_label|default('connexion') }}</button></a>
  <a href="{{ path('index') }}"<button  class="btn btn-danger">{{ button_label|default('retour à la page d\'acceuil') }}</button>   </a>
  
        </div>