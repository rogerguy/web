{% extends 'base.html.twig' %}

{% block title %}Reunion{% endblock %}

{% block body %}
 {% include 'header.html.twig' %}
    <h1>Reunion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reunion.id }}</td>
            </tr>
            <tr>
                <th>membrepresent</th>
                <td>{{ reunion.membrepresent }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ reunion.contenu }}</td>
            </tr>
            <tr>
                <th>membreabsent</th>
                <td>{{ reunion.membreabsent }}</td>
            </tr>

        </tbody>
    </table>
    <div class="form-group" style="margin-top: 30px;margin-right:30px;width:500px ">
                        
    <a href="{{ path('reunion_index') }}"> <input type="button" class="class=btn btn-primary" value="Liste des Reunions"/> </a>
    </div>
                            <div class="form-group" style="margin-top:-45px;margin-left:400px;width:500px ">
                       
                          <a href="{{ path('reunion_edit', {'id': reunion.id}) }}"> <input type="button" class="btnContacte" value="Editer"/> </a>
                        </div>
                        <div class="form-group" style="margin-top:-50px;margin-left:700px;width:500px ">
                          {{ include('reunion/_delete_form.html.twig') }}
                        </div>

     {% include 'footer.html.twig' %}
{% endblock %}
